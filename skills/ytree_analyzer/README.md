# Y-Tree Analyzer 🌳

Навык для иерархического анализа филогенетического Y-дерева.

## Проблема, которую решает

Обычный поиск по терминальному SNP пропускает образцы из:
- Соседних ветвей (сиблингов)
- Дочерних ветвей
- Родительской ветви (недо-тестированные образцы)

**Пример:** Поиск только `J-FT251326` найдёт 2 образца, но пропустит:
- `J-BY87289` (2 образца) — сиблинг
- `J-Y234046` (3 образца) — потомок
- `J-Y94477*` (4 образца) — родительская ветвь

**Итого:** 2 vs **13 образцов** при правильном поиске!

## Использование

### Командная строка

```bash
# Полный анализ одной ветви
python3 ytree_analyzer.py J-Y94477

# Сравнение нескольких ветвей
python3 ytree_analyzer.py J-FT251326 J-BY87289 J-Y94477
```

### Как Python-модуль

```python
from ytree_analyzer import (
    get_haplogroup_path,
    get_sibling_clades,
    search_aadna_for_clade,
    print_tree_analysis
)

# Получить полный путь
path = get_haplogroup_path('J-Y94477')
print(path['path']['string'])

# Найти сиблингов
siblings = get_sibling_clades('J-FT251326')
print(siblings)  # ['J-BY87289']

# Поиск в AADNA с учётом дерева
samples = search_aadna_for_clade('J-Y94477')
print(f"Найдено образцов: {len(samples)}")

# Полный анализ
print_tree_analysis('J-Y94477')
```

## Что делает навык

1. **Получает полный филогенетический путь** от Y-Adam до целевого SNP
2. **Находит родительскую ветвь** (один уровень выше)
3. **Находит все дочерние ветви** (рекурсивно, до 5 уровней)
4. **Находит сиблингов** (другие дети того же родителя)
5. **Ищет в AADNA** по всем связанным ветвям одновременно
6. **Агрегирует результаты** без дублирования по Kit Number

## API Endpoints

Использует Haploserver API:

- `GET /api/search/{SNP}` — детали, TMRCA, дети, полный путь
- `GET /api/haplogroup-path/{SNP}` — альтернативный путь
- `GET /api/subclades/{SNP}` — дочерние ветви

## Пример вывода

```
Y-ДЕРЕВО АНАЛИЗ: J-Y94477
==========================================================================================

📍 ПОЛНЫЙ ФИЛОГЕНЕТИЧЕСКИЙ ПУТЬ:
A0-T > A1 > A1b > BT > CT > CF > F > GHIJK > HIJK > IJK > IJ > J > J2 > J-M410 > 
J-PF4610 > J-L26 > J-PF5087 > J-PF5160 > J-L24 > J-Y22662 > J-L25 > J-Z438 > 
J-Z387 > J-Y17949 > J-FGC35461 > J-FGC34168 > J-FGC35489 > J-Y126071 > J-Z39975 > 
J-Z39973 > J-Z39976 > J-Y94477

📊 ВОЗРАСТ И TMRCA:
TMRCA: 900 лет назад
Сформировалась: 2300 лет назад

🌳 СОСЕДНИЕ ВЕТВИ (сиблинги):
Родительская ветвь: J-Z39976
Сиблинги (2):
  └── J-Z39976*
  └── J-Z39963

🧬 ОБРАЗЦЫ В AADNA (с учётом дерева):
Всего образцов в кладе (включая сиблинги): 13

Распределение по гаплогруппам:
  J-Y94477: 4
  J-Y234046: 3
  J-BY10952: 2
  J-FT251326: 2
  J-BY87289: 2

Фамилии (8):
  - Бекшоков, Догужоко, Кодзоков, Макуашев, Махов, Метов, Пижинов, Шаков

📉 ДОЧЕРНИЕ ВЕТВИ:
  J-Y94477* — TMRCA: 900 лет
  J-FT251326 — TMRCA: 175 лет
  J-Z45304 — TMRCA: 650 лет
```

## Интеграция с другими навыками

- **haploserver**: Использует тот же API
- **aadna_table**: Агрегирует данные из AADNA
- **genetic_analyst**: Предоставляет tree-aware данные для анализа

## Зависимости

- Python 3.7+
- requests (стандартная библиотека)
- Никаких внешних зависимостей!

## Лицензия

Часть OpenClaw workspace skills.
